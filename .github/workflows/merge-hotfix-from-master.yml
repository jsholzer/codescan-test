name: Merge Hotfix from Master

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge-hotfix:
    uses: ./.github/workflows/merge-hotfix.yml
    secrets: inherit
    with:
      source_branch: master